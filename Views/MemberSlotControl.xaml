<UserControl
    x:Class="DiscordAvatars.Views.MemberSlotControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Border BorderBrush="Gray"
            BorderThickness="1"
            Padding="12">
        <Grid RowSpacing="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <CheckBox Content="Activo" IsChecked="{Binding IsActive, Mode=TwoWay}" />

            <ContentControl Grid.Row="1" IsEnabled="{Binding IsActive}">
                <StackPanel>
                    <ComboBox
                        ItemsSource="{Binding FilteredMembers}"
                        IsEditable="True"
                        Text="{Binding SearchText, Mode=TwoWay}"
                        SelectedItem="{Binding SelectedMember, Mode=TwoWay}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DisplayName}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <TextBlock Text="{Binding DisplayName}" FontSize="16" />
                </StackPanel>
            </ContentControl>

            <ContentControl Grid.Row="2" IsEnabled="{Binding IsActive}">
                <Border
                    Width="96"
                    Height="96"
                    BorderThickness="1"
                    BorderBrush="Gray">
                    <Image Source="{Binding AvatarImage}" Stretch="UniformToFill" />
                </Border>
            </ContentControl>
        </Grid>
    </Border>
</UserControl>
